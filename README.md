# Peer Support AI Buddy
Static site (HTML/CSS/JS) + Netlify Functions.

## Local Dev
Open `index.html` in a browser.

## Deploy
Connected to Netlify. Push to `main` redeploys automatically.

## Secrets
API keys are stored in Netlify Environment Variables. Do not commit keys.
